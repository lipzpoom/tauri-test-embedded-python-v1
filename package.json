{
  "name": "test-embedded",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "setup-python": "powershell -ExecutionPolicy Bypass -File setup-portable-python.ps1",
    "build:bundle": "npm run setup-python && npm run build && tauri build",
    "dev:with-python": "npm run setup-python && npm run tauri:dev"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.3",
    "@tauri-apps/cli": "^2"
  }
}
